/** 项目主题色（与 src/styles/variables.less 保持一致，对齐 Element Plus） */
export const THEME_COLORS = {
  primary: "#409eff",
  primaryDark: "#337ecc",
  primaryDarker: "#2667b5",
  danger: "#ee0a24"
};

/** Vant ConfigProvider theme-vars */
export const vantThemeVars = {
  primaryColor: THEME_COLORS.primary,
  successColor: "#67c23a",
  dangerColor: THEME_COLORS.danger
};
